baseUrl: https://satbackend-production.up.railway.app

## API Refactoring To-Do List

### 1. Update `src/api/real/index.ts` to align with the provided API endpoints and payloads:

- [ ] **Auth APIs:**
    - [ ] Change `auth.register` to `POST /user`.
    - [ ] Change `auth.login` to `POST /user/login`.

- [ ] **Teacher/Admin APIs:**
    - [ ] Change `teacher.getUser` from `/admin/users/:id` to `GET /user/:id`.
    - [ ] Change `teacher.getUsers` from `/admin/users` to `GET /user`.
    - [ ] Change `teacher.grantAccess` from `/admin/access` to `POST /user-access`.
    - [ ] Change `teacher.revokeAccess` from `/admin/access/:userId/:testId` to `DELETE /user-access/:id`. (Requires determining how to get the `user-access` ID for deletion).
    - [ ] Align `teacher.createTest` payload with `POST /tests` (only `nomi`, `subject`).
    - [ ] Align `teacher.updateTest` payload with `PATCH /tests/:id`.
    - [ ] Align `teacher.addQuestionToTest` payload with `POST /questions` (test_id, question, type, options, correct_answer, questionImage).

- [ ] **Student APIs:**
    - [ ] Align `student.getTest` payload with `GET /tests/:id` and handle its return structure (e.g., `userAccesses` and `userInfos`).
    - [ ] Align `student.getTests` payload with `GET /tests`.

### 2. Refactor frontend components to align with updated API payloads:

- [ ] **`TestCreationPage.tsx`:**
    - [ ] Adjust the `testData` state and `handleSaveTest` to match the `POST /tests` payload (only `nomi` and `subject` are sent on creation; `savollar_soni`, `is_premium`, `jami_urinishlar` are not sent on initial creation).

- [ ] **`TestEditPage.tsx`:**
    - [ ] Adjust the `useEffect` fetch for `api.student.getTest` to handle the new return structure from `GET /tests/:id`.
    - [ ] Modify `handleSaveQuestions` to ensure the `POST /questions` payload is correct (especially `options` format as an object and `correct_answer` as an object).

### 3. Review and potentially update `types.ts` in `src/api/real/` to reflect new API response structures.

### 4. Remove any "New Request" placeholders or unused API functions if they are not needed.